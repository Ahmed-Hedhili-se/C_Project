name: C CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install GCC
        run: sudo apt-get update && sudo apt-get install -y gcc

      - name: Compile project
        run: gcc browser_history.c test.c -o test -Wall -Wextra

      - name: Run tests
        run: ./test

      - name: Check if executable works
        run: |
          if [ -f "./test" ]; then
            echo "✅ Compilation successful - executable created"
            ./test || echo "⚠️  Program ran but may have issues"
          else
            echo "❌ Compilation failed - no executable found"
            exit 1
          fi